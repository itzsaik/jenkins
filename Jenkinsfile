pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                echo "Jenkins pipeline !"
		echo "Jenkins pipeline !"
		echo " Saikarthik "
		echo " Saikarthik "
            }
        }
    }
}
